require 'sinatra/activerecord/rake'

# Define a default task
task default: :db_tasks

# This task can be customized or expanded as needed
task :db_tasks do
  puts "Running database tasks..."
  # You can add specific database migration or seed tasks here
end

